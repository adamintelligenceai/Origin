import { describe, expect, it } from "vitest";

describe("mobile scaffold", () => {
  it("is a companion shell in phase 0", () => {
    expect(["Today", "Decisions", "Chief"]).toContain("Today");
  });
});
