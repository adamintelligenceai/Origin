import { describe, expect, it } from 'vitest';

describe('privacy tests (placeholder)', () => {
  it('documents local-first architecture requirement', () => {
    const rawRowUploadPolicy = 'browser-local-only';
    expect(rawRowUploadPolicy).toBe('browser-local-only');
  });
});
