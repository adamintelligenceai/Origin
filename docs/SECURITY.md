# Security

MarginShield uses privacy-by-design and local-first processing for transaction files.

- No raw row uploads in v1
- No request-body logging on sensitive routes
- Secrets in deployment environment only
- Security headers on application routes (Phase 10)

See `docs/THREAT_MODEL.md` for the full threat model.
